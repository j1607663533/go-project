# 前端注册页面使用说明

## ✅ 已完成功能

### 注册页面 (`/register`)

- ✅ 漂亮的渐变背景设计
- ✅ Material-UI 组件
- ✅ 完整的表单字段
  - 用户名（必填）
  - 邮箱（必填）
  - 密码（必填）
  - 确认密码（必填）
  - 昵称（可选）
  - 验证码（必填）
- ✅ 实时表单验证
- ✅ 密码显示/隐藏切换
- ✅ 验证码刷新功能
- ✅ 错误提示
- ✅ 加载状态
- ✅ 注册成功提示
- ✅ 自动跳转到登录页面

## 🚀 访问方式

### 1. 直接访问

```
http://localhost:5173/register
```

### 2. 从登录页面跳转

在登录页面点击"立即注册"按钮

### 3. 从任何页面跳转

```javascript
navigate("/register");
```

## 📝 表单字段说明

| 字段     | 必填 | 验证规则                | 说明           |
| -------- | ---- | ----------------------- | -------------- |
| 用户名   | ✅   | 3-20 字符，只能字母数字 | 登录时使用     |
| 邮箱     | ✅   | 有效的邮箱格式          | 用于找回密码等 |
| 密码     | ✅   | 至少 6 个字符           | 登录密码       |
| 确认密码 | ✅   | 必须与密码一致          | 防止输入错误   |
| 昵称     | ⭕   | 最长 50 字符            | 显示名称       |
| 验证码   | ✅   | 6 位数字                | 防止机器人注册 |

## 🎨 页面特性

### 设计亮点

- 渐变紫色背景
- 毛玻璃效果卡片
- 平滑的动画过渡
- 响应式设计

### 功能特性

- **实时验证**

  - 用户名长度和格式检查
  - 邮箱格式验证
  - 密码长度验证
  - 密码一致性检查
  - 验证码长度检查

- **用户体验**
  - 密码可见性切换
  - 点击验证码图片刷新
  - 详细的错误提示
  - 注册成功动画
  - 自动跳转（3 秒）

### 交互细节

- Enter 键提交表单
- 自动聚焦用户名输入框
- 验证失败自动刷新验证码
- 注册成功显示成功页面

## 🔧 使用流程

### 完整的注册流程

```
1. 访问注册页面
   http://localhost:5173/register
   ↓
2. 填写表单
   - 用户名
   - 邮箱
   - 密码
   - 确认密码
   - 昵称（可选）
   - 验证码
   ↓
3. 点击注册按钮
   ↓
4. 前端验证
   - 用户名格式
   - 邮箱格式
   - 密码长度
   - 密码一致性
   - 验证码长度
   ↓
5. 提交到后端
   POST /api/v1/register
   ↓
6. 后端验证
   - 验证码正确性
   - 用户名唯一性
   - 邮箱唯一性
   ↓
7. 注册成功
   - 显示成功页面
   - 3秒后跳转到登录页面
   ↓
8. 使用新账号登录
```

## 💻 代码示例

### 表单验证逻辑

```javascript
const validateForm = () => {
  // 用户名验证
  if (formData.username.length < 3 || formData.username.length > 20) {
    setError("用户名长度必须在3-20个字符之间");
    return false;
  }

  if (!/^[a-zA-Z0-9]+$/.test(formData.username)) {
    setError("用户名只能包含字母和数字");
    return false;
  }

  // 邮箱验证
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(formData.email)) {
    setError("请输入有效的邮箱地址");
    return false;
  }

  // 密码验证
  if (formData.password.length < 6) {
    setError("密码长度至少为6个字符");
    return false;
  }

  // 密码一致性验证
  if (formData.password !== formData.confirmPassword) {
    setError("两次输入的密码不一致");
    return false;
  }

  // 验证码验证
  if (formData.captcha.length !== 6) {
    setError("请输入6位验证码");
    return false;
  }

  return true;
};
```

### 注册提交逻辑

```javascript
const handleSubmit = async (e) => {
  e.preventDefault();

  // 验证表单
  if (!validateForm()) {
    return;
  }

  setLoading(true);

  try {
    // 调用注册 API
    await register(
      formData.username,
      formData.email,
      formData.password,
      formData.nickname,
      captchaData.captcha_id,
      formData.captcha
    );

    // 注册成功
    setSuccess(true);

    // 3秒后跳转到登录页面
    setTimeout(() => {
      navigate("/login");
    }, 3000);
  } catch (err) {
    // 注册失败
    setError(err.message);
    // 刷新验证码
    refreshCaptcha();
  } finally {
    setLoading(false);
  }
};
```

## 🐛 常见问题

### 1. 用户名已存在

**错误**：`用户名已存在`

**原因**：该用户名已被其他用户注册

**解决**：更换一个用户名

### 2. 邮箱已存在

**错误**：`邮箱已存在`

**原因**：该邮箱已被其他用户注册

**解决**：

- 使用其他邮箱
- 或者直接登录（如果是你自己的邮箱）

### 3. 验证码错误

**错误**：`验证码错误或已过期`

**原因**：

- 验证码输入错误
- 验证码已过期（10 分钟）
- 验证码已被使用

**解决**：

- 点击验证码图片刷新
- 重新输入正确的验证码

### 4. 密码不一致

**错误**：`两次输入的密码不一致`

**原因**：密码和确认密码输入不同

**解决**：确保两次输入的密码完全一致

### 5. 用户名格式错误

**错误**：`用户名只能包含字母和数字`

**原因**：用户名包含特殊字符或中文

**解决**：只使用字母（a-z, A-Z）和数字（0-9）

## 📊 错误提示

| 错误信息                         | 原因             | 解决方法              |
| -------------------------------- | ---------------- | --------------------- |
| 用户名长度必须在 3-20 个字符之间 | 用户名太短或太长 | 调整用户名长度        |
| 用户名只能包含字母和数字         | 包含特殊字符     | 移除特殊字符          |
| 请输入有效的邮箱地址             | 邮箱格式错误     | 使用正确的邮箱格式    |
| 密码长度至少为 6 个字符          | 密码太短         | 增加密码长度          |
| 两次输入的密码不一致             | 确认密码不匹配   | 重新输入确认密码      |
| 请输入 6 位验证码                | 验证码长度错误   | 输入完整的 6 位验证码 |
| 用户名已存在                     | 用户名被占用     | 更换用户名            |
| 邮箱已存在                       | 邮箱被占用       | 更换邮箱或登录        |
| 验证码错误或已过期               | 验证码无效       | 刷新验证码            |

## 🎯 注册成功后

注册成功后会：

1. **显示成功页面**

   - 绿色的成功图标
   - "注册成功！"提示
   - "即将跳转到登录页面..."文字
   - 加载动画

2. **自动跳转**

   - 3 秒后自动跳转到登录页面
   - 使用新注册的账号登录

3. **登录流程**
   - 输入刚注册的用户名和密码
   - 输入验证码
   - 登录成功后进入首页

## 🔗 页面跳转

### 从注册页面到登录页面

```javascript
// 点击"立即登录"按钮
navigate("/login");

// 或注册成功后自动跳转
setTimeout(() => {
  navigate("/login");
}, 3000);
```

### 从登录页面到注册页面

```javascript
// 点击"立即注册"按钮
navigate("/register");
```

## 📁 相关文件

```
src/
├── api/
│   └── auth.js          # 注册 API（已更新）
├── pages/
│   ├── Register.jsx     # 注册页面 ⭐ 新增
│   ├── Login.jsx        # 登录页面
│   └── Home.jsx         # 首页
└── main.jsx             # 路由配置（已更新）
```

## 🎨 自定义样式

如果需要修改样式，可以在 `Register.jsx` 中调整：

```javascript
// 修改背景渐变
background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)'

// 修改按钮样式
sx={{
  background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
}}
```

## 📚 相关文档

- 后端注册接口：`backend/REGISTER.md`
- 登录功能：`FRONTEND_LOGIN.md`
- 路由配置：`ROUTER_SETUP.md`

---

**创建时间**: 2025-12-25  
**技术栈**: React 19 + Material-UI 7 + React Router 7  
**状态**: ✅ 完成
